# ha

Личные Home Assistant blueprints и связанные конфиги/скрипты.

## Архитектура климат-контроля

Два слоя:

```
Арбитр направления (автоматизация «Кондиционер: арбитр направления мультисплита»)
    читает температуры, скорости, уставки и ПРИОРИТЕТЫ всех комнат + сезон
    пишет  input_select.konditsioner_napravlenie_sistemy  (Охлаждение|Обогрев|Простой)
                              ↓
Blueprint ac_climate_control.yaml (по автоматизации на комнату)
    крутит duty-cycle только в разрешённую направлением сторону
```

Внутренние блоки мультисплита висят на одном наружном блоке: контур хладагента
один, поэтому **физически невозможно** греть одним блоком и одновременно
холодить или осушать другим. HA об этом ничего не знает — `heat` доступен на
каждом блоке, и любое сочетание режимов прошло бы без возражений. Поэтому
единое направление контура выбирается централизованно, а blueprint следит,
чтобы блок не залез в запрещённую сторону.

### Blueprints

#### `blueprints/automation/stellalupus/ac_climate_control.yaml` (v3, актуальный)

Климат-контроль кондиционера: **time-proportioning (duty-cycle)** вместо
простого bang-bang гистерезиса, с поддержкой обогрева.

На каждом такте пересчитывается «скважность» (duty) — доля времени в текущем
окне, которую блок должен провести в рабочем режиме, пропорционально отклонению
ПРОГНОЗИРУЕМОЙ (по производной, с горизонтом упреждения) температуры от цели.
Прогноз гасит тепловую инерцию: после команды «хватит» комната по инерции ещё
идёт на 0.3–0.6 °C дальше.

Целевая температура у комнаты **одна на оба направления** — и охлаждение, и
обогрев ведут её к этой температуре. Отдельная величина «порог входа в обогрев»
задаёт только условие входа: ниже неё комната начинает греться (и разворачивает
контур через арбитра), но греет уже до целевой. Математика одна на оба
направления, меняются только знак ошибки и рабочий режим:

| Направление | error | Рабочий режим | Off-фаза |
|---|---|---|---|
| Охлаждение | прогноз − целевая | `cool` | `dry` если влажно и комната не ушла ниже цели, иначе `off` |
| Обогрев | целевая − прогноз | `heat` | `off` |
| Простой | — | — | `off` |

**Осушение ограничено по температуре.** `dry` — это работа компрессора на
холод, но без цели и без выхода по температуре: единственный выход из него
это влажность. Как off-фаза duty-цикла оно уводило комнату на 1.5–3 °C ниже
цели, пробивало порог входа в обогрев и разворачивало весь контур —
«пауза» морозила сильнее, чем сама рабочая фаза. Поэтому осушение
разрешено только пока прогноз выше `цель − допуск` (вход «Допустимый увод
вниз при осушении», по умолчанию 0.5 °C), и гасится по этому условию
немедленно, без ожидания минимального интервала: иначе барьер дорисовывает
ещё градус увода вниз, пока досчитывает.

**Часы фазы и часы компрессора — разные величины.** Отсчёт фазы duty-цикла
ведётся только по собственной метке `input_datetime`. Раньше он брался как
`min(метка, last_changed climate-сущности)`, но climate-сущность — зеркало
облака, и её `last_changed` двигает кто угодно: внутренний термостат блока,
отскок облака, пульт, кратковременный `unavailable`. Каждое такое событие
взводило барьер минимального интервала заново, и при duty = 1.0 блок стоял
выключенным часами. `min()` с `last_changed` сохранён отдельной величиной и
используется там, где смысл именно в защите железа от частых пусков:
минимальное время работы и барьер осушки. У арбитра тот же приём с `min()`
законен — там он защищает его собственный выход, который никто, кроме
человека, не пишет.

**Уставке гарантируется направление, а не величина** (вход «Зазор уставки от
датчика блока», по умолчанию 1 °C). Уставка обязана оставаться ниже показаний
встроенного датчика кондиционера на этот зазор в охлаждении и выше — в
обогреве. Смысл offset-трюка в том, чтобы блок **работал**; останавливает его
duty-цикл, а не внутренний термостат.

Промежуточная версия ограничивала поправку по модулю — и ломала сам механизм.
Встроенный датчик меряет обратный воздух и в работе читает на 3–9 °C ниже
комнаты, поэтому обрезанная поправка давала уставку **выше** его показаний:
блок глушил компрессор собственным термостатом, HA видел режим `cool`, а
комната грелась. Наблюдалось: 38 минут в `cool` с уставкой 22 при датчике
блока 21, комната за это время выросла с 24.3 до 24.9. Переохлаждение, ради
которого вводилось ограничение, лечится не здесь, а часами фазы.

Предел остаётся один, аппаратный: уставка не опускается ниже 16 °C. Если
встроенный датчик читает ниже этого (наблюдалось 14 °C), заставить блок
работать через уставку невозможно в принципе.

**Автоматика сама `fan_only` никогда не выбирает.** Вентиляция без компрессора
ничего не даёт и шумит, а при обогреве ещё и гоняет воздух комнатной температуры,
который из-за обдува ощущается холодным. В паузе блок просто выключается.

Но включённый вручную `fan_only` автоматика и **не гасит**: компрессор он не
занимает, направлению контура не противоречит, поэтому в списке запрещённых
режимов его нет. Из `fan_only` блок уводится только когда реально нужен другой
режим — `cool`, `heat` или `dry`.

Осушение включается **и из состояния `off`**, а не только из активной off-фазы:
иначе блок, оказавшийся выключенным при отсутствии потребности в холоде, уже
никогда бы не начал осушать, даже когда влажность ушла за порог.

Отдельно, троттлящимся offset-трюком, корректируется уставка под реальную
температуру в помещении — компенсация неточного встроенного датчика
кондиционера, с защитой от применения устаревших данных. Формула
`уставка = цель + offset` одинаково верна для обоих направлений: в обоих
случаях блок останавливается по своему внутреннему датчику.

**Защита от конфликта направлений — три слоя:**

1. Ветка аварийного выхода гасит блок, оказавшийся в запрещённом для текущего
   направления режиме — включая `auto`, где блок может сам выбрать нагрев.
   Без таймеров, немедленно.
2. Вход в рабочий режим разрешён, только когда ни один из соседних блоков
   (вход `other_ac_units`) не работает в противоположную сторону.
3. Арбитр не разворачивает контур чаще заданного интервала.

Вместе это даёт безопасный двухфазный разворот без явной оркестрации: при смене
направления слой 1 гасит «неправильный» блок за секунду, а входящий блок ждёт
по слою 2, пока контур освободится.

Смена `hvac_mode` дополнительно защищена собственным таймером минимального
интервала (`input_datetime`, а не `last_changed` climate-сущности) плюс нативным
`condition: state ... for: {seconds: 3}`. Ради сохранения этой нативной защиты
вход и выход для `cool` и `heat` разведены по отдельным веткам с литеральными
`state`, а не сделаны одной веткой с шаблоном.

> **Изменение поведения в v3.** Состояние `off` — рабочая фаза паузы, а не сигнал
> «не трогай». Автоматика **включит кондиционер обратно**, когда появится
> потребность. Чтобы выключить комнату насовсем — гасите её тумблер
> `input_boolean.konditsioner_<комната>_avtomaticheskii_klimat_kontrol`; чтобы
> погасить всю систему — `input_boolean.konditsionery_polnoe_vykliuchenie`.
> Именно потому, что `off` стал рабочим состоянием, полное выключение вынесено
> в явный переключатель, а не выражается установкой кондиционера в `off`.

**Импорт в Home Assistant:**

Settings → Automations & Scenes → Blueprints → Import Blueprint →

```
https://github.com/StellaLupus/ha/blob/main/blueprints/automation/stellalupus/ac_climate_control.yaml
```

или через MCP-инструмент `ha_import_blueprint`.

**Обновление после правок в этом репозитории:** заново открыть тот же URL через
«Re-import blueprint» — `source_url` в файле уже на него указывает. Правки в
репозитории сами по себе в HA не попадают.

#### `blueprints/automation/stellalupus/ac_presence_power.yaml` (не используется)

Изначально включал/выключал кондиционер по присутствию. На практике трекеры
присутствия оказались недостаточно надёжны (ложные geofence-переходы без
GPS-фикса, залипание координат на многие часы) — автоматика иногда выключала
кондиционер, пока человек был дома. Автоматизации на основе этого blueprint'а
**удалены** из HA; файл оставлен на будущее.

### Арбитр направления

Отдельная автоматизация (не blueprint — она одна на дом и обобщена внутри).
Алгоритм работает над **декларативным списком комнат** в переменной `rooms`.

Потребность комнаты:

```
pred = температура + скорость × горизонт_упреждения
хочет тепла  при pred < порог_входа_в_обогрев
хочет холода при pred > целевая
```

Промежуток между порогом и целевой — мёртвая зона, в которой комната ничего не
требует. Если они перепутаны (порог ≥ целевой), потребность в тепле игнорируется.

Выбранный обогрев удерживается, пока pred не дойдёт до **целевой**, а не до
порога: порог отвечает на вопрос «когда разворачивать контур», целевая — «докуда
греть». Пока эти две планки совпадали, контур залипал в обогреве, которого никто
не выполнял: арбитр держал направление, а исполнитель уже не видел потребности.
Комната участвует, только если включён её тумблер и датчик валиден.

**Дельта удержания направления** (−1…+1) применяется только к УЖЕ выбранному
направлению: положительная держит его дольше (отпускает лишь после перелёта
цели на дельту), отрицательная отпускает раньше цели. Это гистерезис, который
не даёт компрессору дребезжать на границе.

**Выбор победителя:** комната с минимальным значением приоритета среди тех, у
кого есть потребность; при равных приоритетах — та, у кого больше отклонение от
цели (детерминированно и физически осмысленно: побеждает та, которой хуже).
Направление победителя становится направлением системы.

**Осушение тоже участвует в выборе направления.** `dry` — это работа компрессора
в сторону охлаждения, поэтому комната, которой нужна осушка, claim-ит
`Охлаждение` — но только как запасной вариант: любая температурная потребность
любой комнаты важнее. Без этого блок без потребности в холоде никогда бы не
начал осушать: направление оставалось бы `Простой`, а в простое `dry` запрещён.

**Сезон** ограничивает результат:

| Режим | Смысл |
|---|---|
| `Лето` | Центрального отопления нет — кондиционер отвечает и за тепло, и за холод |
| `Зима` | Греют батареи — кондиционеру разрешено только охлаждение |

Полное выключение — отдельный тумблер `input_boolean.konditsionery_polnoe_vykliuchenie`:
принудительно держит `Простой`, то есть гасит все блоки.

**Защита компрессора:** прямой разворот `Обогрев ⇄ Охлаждение` разрешён не чаще
заданного интервала (по умолчанию 30 мин). Переход в простой и из простоя —
мгновенный, это не разворот 4-ходового клапана. Метка времени обновляется только
при реальной смене направления, иначе защита обнулялась бы на каждом тике.

**Исключение (`soft_direction`):** если текущее охлаждение держится *только*
осушкой, а не температурной потребностью, разворот в обогрев разрешён немедленно.
Осушение — необязательная опция, и она не должна на полчаса блокировать
настоящую потребность в тепле. Разворот между двумя температурными потребностями
по-прежнему ждёт полный интервал — именно от такого пинг-понга защита и нужна.

Признак «держится только осушкой» — это отсутствие температурной потребности в
холоде **у всех комнат** (`any_cool_demand`), а не проигрыш охлаждения в
арбитраже. Сравнение с `temp_demand` было ошибкой: `temp_demand` возвращает
победителя по приоритету, поэтому на любом развороте в обогрев он по определению
не равен «Охлаждение» — `soft_direction` оказывался истинным всегда, и
30-минутная защита не работала вообще. Наблюдалось: разворот через 17.5 минуты
при барьере 30, а комната, которая хотела холода, просто проигрывала по
приоритету и молча оставалась без охлаждения.

**Время с последней смены** считается как `min(своя метка, last_changed самого
input_select)`. Только по своей метке защиту можно обойти: смена направления в
обход арбитра (ручная правка селектора) не записывает метку, и следующий разворот
проходит через секунды. Тот же приём, что в blueprint для смены hvac_mode.

**Подтверждение разворота.** Встречная потребность должна продержаться заданный
интервал (по умолчанию 3 мин), прежде чем контур развернётся: пока её нет,
счётчик длительности сбрасывается на каждом тике. Без этого достаточно одного
всплеска — открыли окно, дунуло на датчик, дребезг показаний — чтобы контур
развернулся и заблокировал возврат на 30 минут.

> `input_select.konditsioner_napravlenie_sistemy` — **выход арбитра, а не орган
> управления.** Менять его вручную бесполезно: арбитр перезапишет значение на
> ближайшем тике (в пределах минуты). Чтобы задать поведение, меняйте уставки,
> приоритеты, сезон или тумблер полного выключения.

### Как добавить третий кондиционер

1. Создать покомнатные хелперы по образцу существующих: целевая температура
   (одна на оба направления), порог входа в обогрев, приоритет, ширина зоны, порог и гистерезис влажности,
   мин. интервал смены режима, гистерезис коррекции уставки, тумблер автоматики,
   две метки времени (`input_datetime`), derivative-сенсор скорости.
2. Добавить запись в список `rooms` арбитра — логика арбитра не меняется.
3. Создать автоматизацию из blueprint'а; в `other_ac_units` указать **оба**
   остальных блока.
4. Дополнить `other_ac_units` у двух существующих автоматизаций новым блоком —
   иначе они не будут его «видеть» при проверке конфликта.
5. Добавить секцию на дашборд.

### Общие хелперы (одни на всю систему)

| Хелпер | Назначение |
|---|---|
| `input_select.konditsioner_sezonnyi_rezhim` | Лето / Зима |
| `input_boolean.konditsionery_polnoe_vykliuchenie` | Полное выключение всех блоков |
| `input_select.konditsioner_napravlenie_sistemy` | Направление контура (пишет арбитр) |
| `input_number.konditsioner_delta_uderzhaniia_napravleniia` | Дельта удержания, −1…+1 |
| `input_number.konditsioner_min_interval_razvorota_kompressora` | Мин. интервал разворота, мин |
| `input_number.konditsioner_podtverzhdenie_razvorota` | Сколько встречная потребность должна продержаться до разворота, мин |
| `input_datetime.konditsioner_metka_vremeni_zaprosa_razvorota` | Служебная метка: когда встречная потребность появилась |
| `input_number.konditsioner_gorizont_uprezhdeniia` | Горизонт упреждения, мин — **общий с blueprint'ом**: если бы они разошлись, арбитр разворачивал бы контур не тогда, когда комнаты этого ждут |
| `input_datetime.konditsioner_metka_vremeni_smeny_napravleniia` | Служебная метка для защиты разворота |
